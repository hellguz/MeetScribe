# <./backend\requirements.txt>
fastapi==0.111.0
uvicorn[standard]==0.29.0

sqlmodel==0.0.16
pydantic-settings==2.3.1
python-multipart==0.0.9

# speech-to-text
faster-whisper==1.1.1

# OpenAI & deps
openai>=1.58.0               # Latest version with responses API and GPT-5 support
httpx                        # Latest httpx for new OpenAI features

# Celery & Redis
celery==5.4.0
redis==5.0.7
sqlalchemy>=1.4 # Already a dependency of sqlmodel, but good to be explicit for func.count

groq==0.26.0

# Language detection
langdetect==1.0.9
